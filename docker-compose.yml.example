services:
  pontus-mail:
    build: .
    container_name: pontus-mail
    restart: always
    ports:
      - "80:3000"
    volumes:
      - ./register.log:/usr/src/app/register.log
    environment:
      - NODE_ENV=production